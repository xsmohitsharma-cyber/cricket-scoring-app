{
  "frontend/src/pages/LiveScoring.tsx": "Live scoring page using default imports for all components, computing total runs directly from the deliveries array via `computeTotalRunsFromDeliveries`, and automatically triggering innings transition (EndOfInningsModal for 1st innings end, scorecard navigation for 2nd innings end) when overs bowled reaches the match overs limit.",
  "frontend/src/pages/Scorecard.tsx": "Full scorecard page using `computeTotalRunsFromDeliveries` for accurate innings totals, correctly resolving batting/bowling teams per innings from match data, and displaying batting/bowling stats with proper player attribution.",
  "frontend/src/pages/MatchHistory.tsx": "Match history page using the updated `StoredMatch` type that includes `isFinished`, fixing the TypeScript errors on `match.isFinished`.",
  "frontend/src/pages/Rules.tsx": "Cricket rules page updated to import `useGetTournamentRules` instead of the removed `useTournamentRules` alias.",
  "frontend/src/data/cricketRules.ts": "Contains the static `cricketRules` array (unchanged) for standard cricket rules, plus a new `getTournamentRules(config: TournamentRules)` function that generates dynamic, config-driven tournament rule categories with template-literal interpolation of all configurable values.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for teams, matches, deliveries, tournament rules, and mutations; `useCreateMatch` now accepts a `toss: Toss` parameter and passes it to `actor.createMatch`, with the `Toss` type imported from `@/backend`.",
  "frontend/src/components/ConfigureTournamentRulesModal.tsx": "Tournament rules configuration modal updated to import `useGetTournamentRules` instead of the removed `useTournamentRules` alias.",
  "frontend/src/App.tsx": "Main app router using TanStack Router with all routes (/, /history, /teams, /setup, /match/$matchId, /live, /scorecard/$matchId, /scorecard, /rules) registered under a root route that wraps everything in QueryClientProvider and Layout with Outlet.",
  "frontend/src/components/Layout.tsx": "App shell that renders a sticky header, main content area via Outlet (or children prop fallback), BottomTabNavigation, and a footer with caffeine.ai attribution.",
  "frontend/src/components/BottomTabNavigation.tsx": "Fixed bottom navigation bar with four tabs (History, Teams, Setup, Rules) using TanStack Router Link components with correct route paths and active state detection.",
  "frontend/src/pages/MatchSetup.tsx": "Five-step match setup wizard (teams → playing11 → toss → rules → confirm) with the toss object built using `TossChoice` enum values directly (not `__kind__` objects), fixing the type mismatch when passing toss to `createMatch`.",
  "frontend/src/components/PlayingElevenSelector.tsx": "New reusable component that displays a team's full squad as selectable checkboxes with a live counter (e.g., \"7 / 11 selected\"), enforces a maximum of 11 selections, and shows visual feedback for selected/disabled/complete states.",
  "frontend/src/lib/matchStore.ts": "localStorage utilities for stored matches, match meta, and match state; `StoredMatch` now includes `isFinished: boolean`; exports both `saveStoredMatch` and the `storeMatch` alias; `saveMatchMeta`/`getMatchMeta` now take/return a `MatchMeta` object with `currentMatchId`.",
  "frontend/src/components/EndOfOverModal.tsx": "End-of-over modal that uses standard Dialog + DialogContent (without manual DialogPortal/DialogOverlay) to prevent double-overlay black screen; prompts for next bowler selection after every 6 legal balls.",
  "frontend/src/components/WicketModal.tsx": "Wicket modal using standard Dialog + DialogContent (without manual DialogPortal/DialogOverlay) to prevent double-overlay black screen; handles dismissal type and next batsman selection.",
  "frontend/src/components/EndOfInningsModal.tsx": "Innings/opener selection modal using standard Dialog + DialogContent (without manual DialogPortal/DialogOverlay) to prevent double-overlay black screen; handles striker, non-striker, and bowler selection for innings start.",
  "frontend/src/hooks/useMatchState.ts": "Custom hook managing live match state (striker, non-striker, bowler, innings, over/ball counts, free hit, match result) with BigInt-safe localStorage persistence, debounced saves, and a `currentOver` field tracked alongside `legalBallsInOver` for innings-end detection.",
  "frontend/src/components/ResetAllDataModal.tsx": "Confirmation AlertDialog modal for the \"Reset All Data\" action; displays a warning icon, a bulleted list of what will be deleted, and two actions — Cancel and a destructive \"Delete All Data\" button with an inline spinner while the reset is in progress.",
  "frontend/src/pages/Teams.tsx": "Teams management page updated to use QueryErrorState component for the getAllTeams error state, showing a friendly message and Retry button (via refetch) instead of raw IC rejection text.",
  "frontend/src/components/BulkPlayerUploadModal.tsx": "Three-step modal (input → preview → result) for bulk-adding players to a team via pasted text or .txt/.csv file upload; validates names for length, spaces, and duplicates, shows a preview list with skip reasons, then adds all valid players via the addPlayer mutation and displays a summary — the 11-player cap has been removed.",
  "frontend/src/components/ScoreDisplay.tsx": "Score display card with the original prop interface (battingTeamName, runs, wickets, overs, etc.) plus an exported `computeTotalRunsFromDeliveries` helper that correctly sums all delivery runs including extras for accurate live totals.",
  "frontend/src/components/OverTimeline.tsx": "Over timeline showing last 3 overs with correct 6-ball slots per over; wides and no-balls don't consume legal ball slots but are displayed; empty slots shown as dashed circles.",
  "frontend/src/components/QueryErrorState.tsx": "Reusable error display component that distinguishes infrastructure errors from application errors, supports custom titles/descriptions/retry labels, and accepts an optional secondary action button for recovery flows.",
  "frontend/src/components/BatsmanStatsPanel.tsx": "Batsman stats panel that accepts an optional allPlayers fallback array for name lookup, ensuring player names resolve correctly even when the primary players array is incomplete.",
  "frontend/src/components/BowlerStatsPanel.tsx": "Bowler stats panel that accepts an optional allPlayers fallback array for name lookup, ensuring the bowler name resolves correctly even when the primary players array is incomplete.",
  "frontend/src/lib/matchUtils.ts": "Utility functions for cricket stats calculation — `calculateInningsStats` correctly sums all delivery runs (including extras) from a Delivery array, `calculateBatsmanStats` and `calculateBowlerStats` compute per-player stats, and `isInningsComplete` checks if overs limit has been reached."
}